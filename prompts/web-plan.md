Use plan mode to design a new page for my existing portfolio website.

## Constraints

- HTML, CSS, and vanilla JS only. No Node.js, no Python, no frameworks.
- External libraries only via CDN.
- Must follow the same design system (colors, fonts, spacing) defined in the existing index.css variables.
- The new page must link back to index.html and share the same navigation style.

## Context

- Existing files: index.html, index.css, index.js
- The site is a personal portfolio with a dark theme and CSS custom properties.

## Task

Plan the creation of a new page called [PAGE_NAME].html that will serve as [DESCRIBE THE PURPOSE — e.g., "a blog page", "a projects gallery", "a contact form page", "a resume/CV page"].

Include in your plan:

- What sections the page needs
- What new CSS will be added (or reused)
- What JS interactions it should have
- What files will be created or modified

Do not write code yet — only plan.
